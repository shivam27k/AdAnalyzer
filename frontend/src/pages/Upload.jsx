import { useState } from "react";
import UploadFile from "../components/UploadFile";

const Upload = () => {

    return (
        <div>
            <UploadFile />
        </div>
    );
};

export default Upload;
